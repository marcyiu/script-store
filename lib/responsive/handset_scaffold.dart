import 'package:flutter/material.dart';

class HandsetScaffold extends StatelessWidget {
  const HandsetScaffold({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
